

# build instructions

 - make sure you've setup rust compiler
 - need vcpkg
 - need llvm compiler

 - clone repo (make sure no spaces or fancy characters in the path)
 - cd into repo
 - run "vcpkg install"
 - run "cargo run"
